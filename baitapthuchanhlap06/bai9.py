danh_sach_chan = [2, 4, 6, 8, 10]
for so in danh_sach_chan:
  assert so % 2 == 0, f"Số {so} không phải là số chẵn"
print("Tất cả các số trong danh sách đều là số chẵn")
danh_sach_hon_hop = [2, 4, 5, 8, 10]
for so in danh_sach_hon_hop:
  assert so % 2 == 0, f"Số {so} không phải là số chẵn" 
print("Tất cả các số trong danh sách đều là số chẵn")